import React, { useState } from 'react';
import { PackageMinus, Send, ArrowLeft } from 'lucide-react';

export default function SaidaEstoque({ setPaginaAtual }) {
  const [formData, setFormData] = useState({
    produtoId: '',
    quantidade: '',
    beneficiario: '',
    motivo: 'Atendimento Social',
    dataSaida: new Date().toISOString().split('T')[0]
  });

  const handleSubmit = (e) => {
    e.preventDefault();
    alert('Baixa de stock registada com sucesso!');
    if (setPaginaAtual) setPaginaAtual('dashboard');
  };

  return (
    <div className="max-w-3xl mx-auto space-y-6">
      <div className="flex items-center gap-4">
        <button 
          onClick={() => setPaginaAtual && setPaginaAtual('dashboard')}
          className="p-2 text-slate-500 hover:text-slate-800 hover:bg-slate-100 rounded-lg transition-colors"
        >
          <ArrowLeft className="w-5 h-5" />
        </button>
        <div>
          <h2 className="text-2xl font-bold text-slate-900">Dar Baixa no Estoque (Saída)</h2>
          <p className="text-sm text-slate-600">Registe a entrega de itens para famílias ou instituições beneficiadas.</p>
        </div>
      </div>

      <form onSubmit={handleSubmit} className="bg-white p-6 rounded-xl border border-slate-200 shadow-sm space-y-5">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label className="block text-sm font-medium text-slate-700 mb-1">Selecione o Produto *</label>
            <select 
              required
              value={formData.produtoId}
              onChange={(e) => setFormData({...formData, produtoId: e.target.value})}
              className="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500"
            >
              <option value="">-- Selecione o item --</option>
              <option value="1">Leite Integral 1L (Disponível: 45 un)</option>
              <option value="2">Cesta Básica Tipo 1 (Disponível: 12 un)</option>
              <option value="3">Sabonete 90g (Disponível: 120 un)</option>
              <option value="4">Arroz 5kg (Disponível: 8 un)</option>
            </select>
          </div>

          <div>
            <label className="block text-sm font-medium text-slate-700 mb-1">Quantidade a Retirar *</label>
            <input 
              type="number" 
              min="1"
              required
              placeholder="Ex: 5" 
              value={formData.quantidade}
              onChange={(e) => setFormData({...formData, quantidade: e.target.value})}
              className="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500"
            />
          </div>

          <div>
            <label className="block text-sm font-medium text-slate-700 mb-1">Beneficiário / Destino *</label>
            <input 
              type="text" 
              required
              placeholder="Ex: Família Silva / Asilo Local" 
              value={formData.beneficiario}
              onChange={(e) => setFormData({...formData, beneficiario: e.target.value})}
              className="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500"
            />
          </div>

          <div>
            <label className="block text-sm font-medium text-slate-700 mb-1">Motivo / Finalidade *</label>
            <select 
              value={formData.motivo}
              onChange={(e) => setFormData({...formData, motivo: e.target.value})}
              className="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500"
            >
              <option value="Atendimento Social">Atendimento Social Direto</option>
              <option value="Cultura/Evento">Evento Comunitário</option>
              <option value="Vencimento">Descarte por Validade</option>
              <option value="Transferencia">Transferência de Entidade</option>
            </select>
          </div>
        </div>

        <div className="flex justify-end gap-3 pt-4 border-t border-slate-100">
          <button 
            type="button"
            onClick={() => setPaginaAtual && setPaginaAtual('dashboard')}
            className="px-4 py-2 border border-slate-300 text-slate-700 rounded-lg hover:bg-slate-50 transition-colors text-sm font-medium"
          >
            Cancelar
          </button>
          <button 
            type="submit"
            className="bg-rose-600 hover:bg-rose-700 text-white px-5 py-2 rounded-lg text-sm font-medium flex items-center gap-2 transition-colors shadow-sm"
          >
            <Send className="w-4 h-4" /> Confirmar Saída
          </button>
        </div>
      </form>
    </div>
  );
}